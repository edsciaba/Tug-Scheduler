import React from "react";
import ReactDOM from "react-dom/client";
import TugboatScheduler from "./TugboatScheduler.jsx"; 
import "./index.css"; 

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TugboatScheduler />
  </React.StrictMode>
);
